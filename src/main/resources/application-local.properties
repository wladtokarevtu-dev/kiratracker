# Admin-Login
APP_SECURITY_USERNAME=admin
APP_SECURITY_PASSWORD=kira2024

# H2 In-Memory-Datenbank (keine Installation nötig)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Hibernate soll die Tabellen automatisch erstellen
spring.jpa.hibernate.ddl-auto=create-drop

# H2-Console aktivieren (optional, zum Debuggen)
spring.h2.console.enabled=true

# Weather API Key (dummy für lokale Tests)
WEATHER_API_KEY=dummy_key_for_local_testing
